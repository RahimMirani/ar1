[project]
name = "firedrone-tello"
version = "0.1.0"
description = "Tello variant of FireDrone: web dashboard for live control, telemetry, and video"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "djitellopy>=2.5.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "opencv-python>=4.10.0",
]

[tool.uv]
package = false
